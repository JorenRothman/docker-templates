# Payload 3.0 Template

## Docker File

Update:

- `UPLOAD_FOLDER` to correct upload folder
- shared-env stage to include correct environment variables

## Next.js Config

```mjs
const nextConfig = {
  output: "standalone",
};
```
